const { Router } = require('express');
const router = Router();

router.get('/', )

router.get('/:id', )

router.get('/user/:id', )

router.post('/', )

router.patch('/:id', )


router.delete('/:id', )

module.exports = router;