const { Router } = require('express');
const router = Router();

router.get('/user/:id', )

router.get('/:id', )

router.get('/', )

router.get('/saldo/:id', )

router.post('/', )

router.patch('/:id', )

router.delete('/:id', )

router.patch('/saldo/:id', )

module.exports = router;