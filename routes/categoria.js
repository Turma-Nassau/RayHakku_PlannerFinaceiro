const { Router } = require('express');
const router = Router();

router.get('/user/:id', )

router.get('/:id', )

router.get('/', )

router.post('/', )

router.patch('/:id', )

router.delete('/:id', )

module.exports = router;